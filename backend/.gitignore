node_modules
# Keep environment variables and local data out of version control
# .env
# backend/.env

# uploads are committed in this demo repo so GitHub zip works with media out of the box

# database file (SQLite) and prisma client
dev.db
/src/generated/prisma
